Preserved full USB/xHCI/UAS forensic profile
============================================

Use these settings in log_os.rs when a new controller mystery requires the
complete per-command, per-transfer, DMA-remap, and UAS block-I/O trace:

    pub(crate) const USB_UAS_DIAG_PROFILE_ENABLED: bool = true;
    pub(crate) const USB_RUNTIME_DIAG_PROFILE_ENABLED: bool = true;

The derived settings are:

    GLOBAL_LOG_LEVEL = LogLevelPolicy::up(LevelFilter::Trace)
    USB_LOG_LEVEL = LogLevelPolicy::up(LevelFilter::Trace)
    USB_MASS_UAS_TRACE_LOGS = true
    USB_LOG_ALL = AtomicBool::new(true)

Validated full-trace capture:
  bld/baremetal-logs/trueos-baremetal.0.log

That capture proved the Intel 8086:7A60 port-4 reset, the 22d4:1321 HID
enumeration and stream handoff, the protected port-11 policy, and concurrent
152e:7001 SKHYNIX UAS I/O.
