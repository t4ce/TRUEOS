State of the art, current Intel GPU bring-up frontier
======================================================

This file is a raw proof transcript.  Read it like the HDA log: every line
claims one hardware boundary and does not claim the next boundary.

Current proven claims in this capture:

- `pci-claim`: TrueOS claimed Intel GPU `00:02.0`, device `0x4680`,
  revision `0x0C`, with `0x1000000` bytes of MMIO.
- `guc-proof`: GuC firmware was found and bootstrap reached
  `bootrom=JUMP_PASSED`, `ukernel=READY`, `auth=0x2`.
- `display-proof`: primary boot surface is on `pipe-a`, live surface register
  matches `0x02000000`, and the logo path reports `ok=1`.
- `vertex-upload-proof`: CPU writes the canonical three-vertex triangle,
  reads back the exact bytes, and flushes the upload range before submit.
- `mi-scanout-store-proof`: RCS wrote one DWORD into the exact live scanout
  surface and CPU readback saw the commanded value.
- `vf-proof`: `vf-streamout-proof`, `vf-draw-path`,
  `vs-draw-frontier`, and `vs-streamout-proof` report
  `vf-proof accepted=1` with IA counters moving.
- `clip-raster-proof`: `vf-draw-path` reports
  `clip-raster-proof accepted=1` with `cl_delta=1`, `cl_prim_delta=1`.
- `ps-launch-frontier-proof`: on `vf-draw-path`, the PS launch input frontier
  reports `input_ready=1` while PS/CPS/depth counters all remain zero.
- `vs-proof`: `vs-draw-frontier` and `vs-streamout-proof` report
  `vs-proof accepted=1` with `vs_delta=3`, and shader upload matches the baked
  blob.
- Current negative frontier: `vs-draw-frontier` still reports
  `clip-raster-proof accepted=0` across the logged SBE/URB variants, while the
  VF-only draw path proves the clipper/raster packet frontier can be reached
  without launching PS.

High-level checklist:

| Step | Status | Certainty | Evidence in this capture | What it proves | What it does not prove |

Rows are ordered by proof/execution flow: claim the device, make registers and
firmware sane, own memory, submit work, then walk the 3D pipeline until pixels
or display handoff.

| 01. Claim hardware / PCI | Proven | High | `intel: claimed 00:02.0 device=0x4680 rev=0x0C mmio_len=0x1000000` | TrueOS found and owns the expected Intel GPU function and BAR size. | MMIO access quality, forcewake, memory mappings, or engines. |

| 02. MMIO / forcewake | Mostly proven | Medium-high | Recovery logs end with `forcewake_ok=1`; many MMIO counter/register reads are coherent. | The register path is alive enough for RCS/display diagnostics and recovery. | A dedicated stable-read/write forcewake contract for every needed register island. |

| 03. GuC bootstrap | Proven | High | `bootstrap ready=1 ... bootrom=JUMP_PASSED ukernel=READY auth=0x2` | Firmware placement/auth/bootstrap reached ready state. | That render submission depends on GuC, or that GuC scheduling is being used correctly. |

| 04. GPU memory / GGTT for warm buffers | Partly proven | Medium | Streamout writes, batch/result markers, and display surface evidence all refer to expected GPU addresses. | Several GPU-visible buffers are usable by some blocks. | A single named `memory-proof` line that lists every warm buffer, cache flush, GGTT slot, and readback. |

| 05. Vertex upload | Proven | High | `vertex-upload-proof accepted=1 stage=cpu-write-readback bytes=36 stride=12 count=3 gpu=0x870000 readback_ok=1 flush=1` | CPU wrote the canonical triangle, read back the exact bytes, and flushed the upload range before submit. | VF fetch, shader execution, clipper progress, PS dispatch, or pixels. |

| 06. Shader upload | Proven | High | `shader-upload-verify ... vs_match=1 ps_match=1`; baked/uploaded signatures match for both VS and PS blobs. | The shader bytes used by the batch match the baked Mesa-derived blobs in memory. | That the VS output contract is correct, or that the PS ever dispatches. |

| 07. Batch submit / RCS markers | Partly proven | Medium | New `batch-submit-proof` lines show `start_marker=1`; some paths also show `post3d_marker=1`; all current paths have `final_marker=0`. | RCS accepts enough command stream to run markers and some 3D packets. | Full retire; current traces still end `completed=0` / `final_marker=0`. |

| 07a. RCS MI scanout store | Proven | High | `mi-scanout-store-proof accepted=1 completed=1 marker=0xC0DE7713 xy=1280x720 gpu=0x2709400 pitch=0x2800 before=0x00D9D9D9 after=0x00262626 color=0x00262626`. | RCS command streamer can write one DWORD into the exact live scanout surface through GGTT. | VF, VS, clip/raster, PS dispatch, color backend, or render-target export. |

| 08. VF / input assembler | Proven | High | `vf-proof accepted=1 ia_vtx_delta=3`; streamout path also shows `delta_so0=3 delta_so_write0=96` with `delta_vs=0`. | VF can consume the declared vertex layout; one proof streams out without VS. | VS, clipper, PS, or render target writes. |

| 09. VS execution | Proven | High | `vs-proof accepted=1 vs_delta=3` on `vs-draw-frontier` and `vs-streamout-proof`. | VS counters move for the three submitted vertices. | Correct VS output layout into clipper/SBE; VS paths still do not produce clipper counters. |

| 10a. Clipper counter | Proven on VF path | High | `vf-draw-path clip-counter-proof accepted=1 cl_delta=1 cl_prim_delta=1`. | The clipper accepts/generates primitive work when fed by the VF-only path. | Raster sample generation, PS launch, or VS-to-clipper handoff. |

| 10b. Raster packet frontier | Proven on VF path | Medium-high | `vf-draw-path raster-packet-proof accepted=1 post_clip=0xC0DE7726 post_raster=0xC0DE7727 clip_counter=1 sc_instdone=0xFFFFFFFF`. | The batch reaches raster/post-raster marker points after a clipper counter moved on the VF draw path. | That raster emitted samples/fragments; no standalone raster memory write exists here. |

| 10c. Shape-to-fragment boundary | Pending next boot | High when logged | Expected lines: `fragment-candidate-shape accepted=1 geometry=oversized ... screen_bbox=[0,0..W,H]`, `fragment-candidate-proof ... candidate_ready=1 ... fragment_observed=0`, and `primary-fragment-boundary-gate ... action=halt-ps-spectrum` until a real fragment/WM/PS-facing observable exists. | We give raster a deliberately oversized triangle that covers the full target, then stop the PS-spectrum fanout unless the shape-to-fragment boundary is actually observed. | Actual fragment/sample emission unless `fragment_observed=1`; the current observable is still only a coverage candidate plus clip/raster packet frontier. |

| 11. Pixel shader dispatch | Not proven | High confidence negative through 11g | `vf-draw-path ps-launch-frontier-proof accepted=0 input_ready=1 ps_state_marker=1 raster_packet=1 clip_counter=1 ps_delta=0 cps_delta=0 ps_depth_delta=0`; `ps-dispatch-slot-proof accepted=0 input_ready=1 dispatch_slot=0|1|2 ksp_slot=0|1|2 ...`; all five `ps-grf-proof` and all five `ps-payload-proof` variants also preserve `input_ready=1` with `ps_delta=0 cps_delta=0 ps_depth_delta=0`. | The clean PS launch question is isolated through dispatch width, KSP location, GRF/start/max-thread knobs, and payload-spectrum variants. | That any PS/CPS thread actually launched, or that any RT/depth write happened. |

| 11a. PS launch with oversized VF primitive | Negative proof | High | `ps-launch-big-primitive-proof accepted=0 input_ready=1 oversized=1 ps_state_marker=1 raster_packet=1 clip_counter=1 ps_delta=0 cps_delta=0 ps_depth_delta=0`. | The row-11 failure is not explained by the small canonical triangle missing coverage; the oversized VF primitive still reaches the PS launch frontier with no PS/CPS/depth counter motion. | Render target write or visible pixels; accepts only on PS/CPS/depth counter movement. |

| 11b. PS binding-table-count variant | Negative proof | High | `ps-bt1-big-primitive-proof accepted=0 input_ready=1 oversized=1 ps_bt_count=1 ps_state_marker=1 raster_packet=1 clip_counter=1 ps_delta=0 cps_delta=0 ps_depth_delta=0`. | The PS launch gap is not fixed by encoding one PS binding table entry; the oversized VF path still reaches the launch frontier with no PS/CPS/depth counter motion. | RT write or visible pixels; accepts only on PS/CPS/depth counter movement. |

| 11c. WM normal dispatch variant | Negative proof | High | `ps-wm-normal-big-primitive-proof accepted=0 input_ready=1 oversized=1 wm_force=normal dispatch_qualifier=writeable_rt ps_state_marker=1 raster_packet=1 clip_counter=1 ps_delta=0 cps_delta=0 ps_depth_delta=0`; backend gate says `dispatch_armed=1 reason=writeable-rt`. | The debug `3DSTATE_WM` force-thread-dispatch-on setting is not the blocker; normal writeable-RT dispatch also reaches the frontier with no PS/CPS/depth counter motion. | RT write or visible pixels; accepts only on PS/CPS/depth counter movement. |

| 11d. PS dispatch-width / KSP-slot encoding | Negative proof | High | Corrected-layout lines: slot0 `ksp0=0xC0 ... ps_dispatch_bits=100`, slot1 `ksp1=0xC0 ... ps_dispatch_bits=010`, slot2 `ksp2=0xC0 ... ps_dispatch_bits=001`; each has `ps-dispatch-slot-proof accepted=0 input_ready=1 ... ps_delta=0 cps_delta=0 ps_depth_delta=0`. | Dispatch width and actual Gen12.5 KSP-slot placement are not the blocker; all single-slot variants preserve the launch frontier but do not launch PS threads. | RT export or visible pixels; accepts only on PS/CPS/depth counter movement. |

| 11e. PS KSP / program-offset contract | Proven | High | `ps-ksp-proof accepted=1 ... first_dw=0xA07E0061 baked_first=0xA07E0061`; corrected slot probes show `ksp0=0xC0`, `ksp1=0xC0`, and `ksp2=0xC0` in their respective variants. | The uploaded baked PS code word is present and each corrected KSP slot points at it when selected. | PS thread launch, shader semantics, or RT export; this is a CPU readback contract, not a PS counter acceptance. |

| 11f. PS GRF / registers-per-thread contract | Negative proof | High | `probe-ps-grf-decoded` confirms the programmed variants: `grf-start-r1` has `ps_dw7=0x00010101`, `grf-start-r2` has `ps_dw7=0x00020202`, `grf-start-r4` has `ps_dw7=0x00040404`, `maxthreads-31` has `ps_dw6=0x0F800001`, and `maxthreads-15` has `ps_dw6=0x07800001`; each `ps-grf-proof` has `accepted=0 input_ready=1 ps_delta=0 cps_delta=0 ps_depth_delta=0`. | The exposed gfx12.5 PS register contract knobs are not the blocker: dispatch GRF start registers and max threads per PSD preserve the launch frontier but do not launch PS threads. There is no separate Gen12.5 `3DSTATE_PS` registers-per-thread field in this packet layout. | RT export or shader output correctness; accepts only on PS/CPS/depth counter movement. |

| 11g. PS scratch / thread-payload contract | Negative proof | High | `probe-ps-payload-decoded` logs prove each variant was programmed; `ps-payload-proof accepted=0 input_ready=1` for `push-constant-enable`, `attribute-enable`, `simple-ps-hint`, `source-depth-w`, and `bary-plane-coeffs`, all with `ps_delta=0 cps_delta=0 ps_depth_delta=0`. | The PS launch gap is not fixed by push-constant enable, attribute payload enable, simple-PS hint, source Z/W payload phases, or barycentric plane payload phases. | RT export or correct color; accepts only on PS/CPS/depth counter movement. |

| 11h. PS_EXTRA dependency/source bits | Blocked by 10c | Medium-high only after fragment boundary | Expected line, once unblocked: `ps-extra-dep-proof accepted=... input_ready=1 ps_extra=... dep_on_cp=... source_depth_w=... bary=... ps_delta=...`. | Tests Gen12/XeLP `3DSTATE_PS_EXTRA` dependency/source/barycentric bits only after the shape-to-fragment boundary is no longer the active miss. | RT export or visible pixels; accepts only on PS/CPS/depth counter movement. |

| 11i. Gen12/XeLP 3DSTATE ordering / flush | Pending | Medium-high when logged | Expected line: `ps-ordering-proof accepted=... input_ready=1 order_variant=... flush_variant=... ps_delta=...`. | Tests whether PS launch depends on packet order or explicit invalidate/flush sequencing around PS, SBE, BLEND, WM, and draw. | Shader correctness or RT export; accepts only on PS/CPS/depth counter movement. |

| 12. Render target write | Not proven | High confidence negative | Every `ps-rt-proof` line has `accepted=0 ps_delta=0 rt_any_change=0 rt_triangle_change=0`, including corrected dispatch-slot, all five GRF/max-thread variants, and all five payload variants. | Sampled pixels did not change in this capture, even after oversized coverage, PS BT-count, WM normal-dispatch, corrected single-slot dispatch, GRF/max-thread spectrum, payload-spectrum tests, dispatch-all control, and KSP readback tests. | Whether the blocker is PS dispatch, backend/color export, RT state, cache visibility, or hang-before-export. |

| 13. Display handoff | Proven | High | `primary-boot-surface ... surf=0x02000000 surf_live=0x02000000 ok=1 logo=1` | The display pipe/plane can scan out the memory we think it can. | That 3D rendered the scanned-out pixels. |

| 14. CPU scanout payload | Proven | High | `cpu-scanout-proof accepted=1 ... xy=2536x1416 size=8x8 before=0x00CACACA after=0x00FF00FF flush=1 surf=0x02000000 surf_live=0x02000000`; visually observed as a pink marker. | CPU can place a simple marker into the exact live scanout surface, and display memory/pitch/format are coherent. | Render backend write, PS dispatch, or that render produced the marker. |

| 15. Displayed rendered pixel | Not proven | High confidence negative | Display proof is independent; `ps-rt-proof accepted=0`. | We can display known memory, and step 14 proves CPU payload placement is correct. | That render produced the displayed memory. |

Certainty key:

- High: one or more direct hardware observables agree, and the failure mode is
  narrow.
- Medium-high: direct observables agree, but the proof is inferred across
  adjacent setup/recovery paths.
- Medium: useful evidence exists, but a named proof line is still missing or
  the success is not repeat-isolated.
- Low: mostly hypothesis or stale archaeology.

Current frontier:

- `mi-scanout-store-proof` is now answered: RCS can write one DWORD into the
  same live scanout surface that CPU/display already proved.  This narrows the
  problem below command-streamer/scanout memory access, without claiming any 3D
  pipeline stage.
- We have not yet proven `ps-rt-proof`.  The VF draw path, the oversized VF
  primitive, the PS binding-table-count variant, and the WM normal-dispatch
  variant all reach `ps-launch-frontier-proof input_ready=1`, but the captures
  below still show `ps-dispatch-proof accepted=0`, `ps-rt-proof accepted=0`,
  `ps_delta=0`, `cps_delta=0`, `ps_depth_delta=0`, and `rt_any_change=0`.
  The dispatch-all variant is intentionally separated because it regresses to
  `input_ready=0`.
- `vf-draw-path` proves clipper primitives, but `vs-draw-frontier` only proves
  VS counters plus packet markers; it does not yet prove real VS-to-clipper
  handoff.
- Row 10/11 is now split into named logs: `clip-counter-proof`,
  `raster-packet-proof`, `ps-launch-frontier-proof`, and
  `ps-launch-big-primitive-proof`.  The crisp current frontier is
  `input_ready=1` with no PS/CPS/depth counter motion, even for oversized
  coverage.
- `ps-bt1-big-primitive-proof` is now answered: the same oversized VF path,
  with 3DSTATE_PS binding table entry count encoded as one, still has
  `input_ready=1` and no PS/CPS/depth counter motion.  Stop blaming PS BT
  count.
- `ps-wm-normal-big-primitive-proof` is now answered: the same oversized VF
  path, with WM force-thread-dispatch left at normal and writeable-RT as the
  dispatch qualifier, still has `input_ready=1` and no PS/CPS/depth counter
  motion.  Stop blaming the WM force-on debug knob.
- `ps-ksp-proof` is answered: CPU readback shows the programmed KSP offset
  points at the baked/uploaded PS code word.  Stop blaming a simple PS code
  upload or KSP offset mistake.
- Corrected `ps-dispatch-slot-proof` is answered: slot0, slot1, and slot2 each
  use the actual Gen12.5 KSP packet location, preserve `input_ready=1`, and
  still report `ps_delta=0 cps_delta=0 ps_depth_delta=0`.
- `ps-dispatch-width-proof` remains useful only as a control.  If corrected
  all-slot dispatch still loses the normal `input_ready=1` frontier, keep
  treating dispatch-all as intentionally aggressive rather than as the main
  evidence path.
- Row 11f is answered as a negative spectrum: GRF start `r1/r2/r4` and
  max-threads-per-PSD `31/15` all preserve `input_ready=1` and still do not
  move PS/CPS/depth counters.
- Row 11g is answered as a negative spectrum: push-constant enable, attribute
  enable, simple-PS hint, source Z/W payload phases, and barycentric plane
  phases all preserve `input_ready=1` and still do not move PS/CPS/depth
  counters.
- The deeper row-11 backlog is now split into named contracts: dispatch-width
  encoding, KSP/program offset, GRF/registers-per-thread, scratch/thread
  payload, PS_EXTRA dependency/source bits, and Gen12/XeLP packet ordering or
  flush sequencing.  Row 11e accepts on CPU readback of the programmed KSP code
  word; the launch rows accept only on PS/CPS/depth counter movement.
- We have not proven "triangle visible because render produced pixels."  The
  display proof is real, but it is independent of the failing PS/render-target
  boundary until `ps-rt-proof` changes a sampled pixel.
- Row 10c is now the active gate.  The boot stops the PS-spectrum fanout after
  the oversized shape probe unless `fragment_observed=1`; until then, further
  PS-side negative probes are treated as blocked rather than meaningful.

Next best patch target:

- First read row 10c on the next boot.  If `primary-fragment-boundary-gate`
  says `action=halt-ps-spectrum`, the next patch is not PS state; it is finding
  or wiring a real rasterizer sample / WM-input observable.
