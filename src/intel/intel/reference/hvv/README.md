TRUEOS HVV Reference
====================

This directory preserves the old HVV nested-virtualization idea as Intel-only
reference material. It is intentionally not wired into the kernel build.

The active TRUEOS VM path lives under `src/hv`. These files remain here only as
notes for a possible future full-TRUEOS-inside-TRUEOS hypervisor direction.
