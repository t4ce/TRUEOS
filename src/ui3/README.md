# UI3

UI3 no longer owns a Pixi-style retained scene host.

The remaining module keeps shared UI assets warm for later render paths. The old
Pixi command host, service task, hit scene, presenter, capture endpoint, and
shell control command have been removed.
