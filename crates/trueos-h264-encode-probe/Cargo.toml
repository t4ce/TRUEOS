[package]
name = "trueos-h264-encode-probe"
version = "0.1.0"
edition = "2024"
license-file = "../../LICENSE"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
less-avc = { version = "0.1.5", default-features = false }
