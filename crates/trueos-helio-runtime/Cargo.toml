[package]
name = "trueos-helio-runtime"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
libm = { version = "0.2", default-features = false }
trueos-helio-artifact = { path = "../trueos-helio-artifact" }

