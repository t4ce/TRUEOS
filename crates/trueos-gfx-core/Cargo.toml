[package]
name = "trueos-gfx-core"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
libm = { version = "0.2", default-features = false }

[features]
default = []
alloc = []
