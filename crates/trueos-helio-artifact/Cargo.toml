[package]
name = "trueos-helio-artifact"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Zero-copy no_std loader for relocatable Helio render artifacts"

[dependencies]
crc32fast = { version = "1.5.0", default-features = false }

