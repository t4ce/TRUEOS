[package]
name = "trueos-fs"
version = "0.0.1"
edition = "2024"

[features]
default = []

[dependencies]
infer = { path = "../../vendor/infer", default-features = false }
trueos-crypto = { path = "../trueos-crypto" }
