[package]
name = "trueos-kokoro-duration"
version = "0.1.0"
edition = "2024"
license-file = "../../LICENSE"
description = "Exact allocation-free Kokoro decoder frame-count resolver for TRUEOS"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
libm = { version = "0.2", default-features = false }
