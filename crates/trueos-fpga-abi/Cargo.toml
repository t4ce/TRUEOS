[package]
name = "trueos-fpga-abi"
version = "0.1.0"
edition = "2024"
license-file = "../../LICENSE"

[lib]
path = "src/lib.rs"

[dependencies]
zerocopy = { version = "0.8.52", default-features = false, features = ["derive"] }
