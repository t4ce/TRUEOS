[package]
name = "trueos-kokoro-lexicon"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Zero-copy sealed Misaki pronunciation lexicon for TRUEOS Kokoro"
readme = "README.md"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
sha2 = { version = "0.10.9", default-features = false }
trueos-kokoro-g2p = { path = "../g2p" }
