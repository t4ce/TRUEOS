[package]
name = "trueos-kokoro-g2p"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Bounded no-std English G2P and Kokoro text frontend for TRUEOS"
readme = "README.md"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
sha2 = { version = "0.10.9", default-features = false }
