[package]
name = "trueos-kokoro-voice"
version = "0.1.0"
edition = "2024"
license-file = "../../../LICENSE"
description = "Zero-copy no-std parser for the sealed TRUEOS Kokoro voice archive"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
sha2 = { workspace = true }
