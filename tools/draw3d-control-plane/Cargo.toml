[package]
name = "draw3d-control-plane"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
eframe = { version = "0.35.0", default-features = false, features = ["default_fonts", "glow", "x11"] }
shlex = "1.3.0"
trueos-draw3d = { path = "../../crates/trueos-draw3d" }

