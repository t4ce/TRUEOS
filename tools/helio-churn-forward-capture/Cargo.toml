[package]
name = "helio-churn-forward-capture"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
bytemuck = { version = "1", features = ["derive"] }
glam = "0.33"
helio-artifact = { path = "../../../helio/crates/helio-artifact" }
libhelio = { path = "../../../helio/crates/libhelio" }
pollster = "0.4"
wgpu = { path = "../../../helio/vendor/wgpu/wgpu", default-features = false, features = ["std", "parking_lot", "vulkan", "gles", "wgsl", "trace"] }

