# 104 — `7a301072001f83ee141a19c1b2538d3907c13121` — 2026-05-16

Original message: `ok`

Hindsight subject: Expose clock and VFS primitives while moving chat toward an app-owned service

Body: `api/src/clock.rs` exported `ntp_current_unix_seconds` through `v::vclock`, `api/src/vfs.rs` added read/read-UTF8/write/create-dir wrappers over `v::vfs`, and the chat Tailwind asset moved beside `apps/chatserver` as the old `apps/servers/chatserver/server.rs` was removed. The flags/currency diagnostics were adjusted in the same pass, preparing 105’s userspace chat server to consume the kernel CABI clock/filesystem path associated with kernel `2d07235d9d82dcb014ef7bb2cd2cc4056731eef8`.

