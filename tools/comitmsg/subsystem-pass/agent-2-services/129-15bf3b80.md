# 129 — `15bf3b808ae13f6b223ec352e32f4d3a11df63d1` — 2026-06-22

Original message: `ok`

Hindsight subject: Expose filesystem listing/stat and external-target metadata to packaged apps

Body: `api/src/lib.rs` expanded the filesystem/net/runtime exports, `apps/hello_world/src/main.rs` declared `trueos_cabi_fs_list_dir` and `trueos_cabi_fs_stat`, and the file explorer backend, `apps/target.json`, `src/build_plan.rs`, and `src/main.rs` adopted the new target/package shape. This turns 128’s facade into a buildable application contract over kernel `084d4bd48a467a6ec49c1ad781fb7830fbdcfbb5`’s `src/r/io/fs_cabi.rs` functions rather than leaving listing/stat as a hand-written probe.

