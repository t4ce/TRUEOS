# 089 — `9d5e1d696593d7b7a383680a48451ea4ab384040` — 2026-05-15

Original message: `ok`

Hindsight subject: Co-locate reusable service crates with their owning applications

Body: The reusable `trueos-currency`, `trueos-flags`, and `trueos-weather` crates moved from `apps/crates/*` beside `currency_reqwest`, `flags`, and `weather`, preserving their source APIs while `src/main.rs` gained the path mappings needed to build them. This is a catalog/build-layout refactor after 088: the network helpers still target the existing `trueos_cabi_net_fetch_bytes_*` contract, and no kernel behavior can be attributed to the move.

