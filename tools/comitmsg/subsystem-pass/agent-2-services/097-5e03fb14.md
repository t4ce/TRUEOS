# 097 — `5e03fb14fc8b78eb2069ce38bed77c116927be26` — 2026-05-15

Original message: `ok`

Hindsight subject: Make the insecure TLS fallback an explicit service state

Body: Currency removed the attempted default reqwest builder and labeled its fallback `reqwest.client.build.insecure_tls`; flags gained the same timeout/debug/source-chain helper and `tls_danger_accept_invalid_certs(true)`. This documents a transport compromise in the consumers from 090 instead of hiding it behind a generic connection failure, setting up the later choice between direct reqwest and worker fetch in 098–103.

