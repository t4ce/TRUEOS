[package]
name = "wgpu-video-mesh"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bytemuck = { version = "1.25", features = ["derive"] }
eframe = { version = "0.35.0", default-features = false, features = ["default_fonts", "wayland", "wgpu", "x11"] }
glam = "0.30"

[profile.dev]
opt-level = 1

