[package]
name = "truega-decode-backend-check"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
trueos-fpga-abi = { path = "../../crates/trueos-fpga-abi" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("trueos"))'] }

[workspace]
