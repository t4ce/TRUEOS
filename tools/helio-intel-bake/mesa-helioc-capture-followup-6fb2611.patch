diff --git a/src/intel/vulkan/anv_descriptor_set.c b/src/intel/vulkan/anv_descriptor_set.c
index a39eb29..ec4b3ca 100644
--- a/src/intel/vulkan/anv_descriptor_set.c
+++ b/src/intel/vulkan/anv_descriptor_set.c
@@ -30,11 +30,14 @@
 #include "util/mesa-blake3.h"
 #include "vk_util.h"
 
+#include <stdatomic.h>
 #include <stdio.h>
 
 #include "anv_private.h"
 
-/* Raw records contain virtual addresses and are capture evidence only. */
+/* Raw records contain virtual addresses and are capture evidence only.  One
+ * per-record filename avoids interleaved append writes. */
+static atomic_uint trueos_helioc_descriptor_sequence = 0;
 static void
 anv_trueos_helioc_dump_descriptor_state(uint32_t kind, VkDescriptorType type,
                                         uint32_t binding, uint32_t element,
@@ -43,10 +46,11 @@ anv_trueos_helioc_dump_descriptor_state(uint32_t kind, VkDescriptorType type,
    const char *dir = os_get_option("TRUEOS_HELIOC_ANV_DUMP_DIR");
    if (dir == NULL || data == NULL || bytes == 0)
       return;
+   const unsigned sequence = atomic_fetch_add(&trueos_helioc_descriptor_sequence, 1);
    char path[4096];
-   if (snprintf(path, sizeof(path), "%s/helioc-anv-state.bin", dir) >= (int)sizeof(path))
+   if (snprintf(path, sizeof(path), "%s/helioc-anv-descriptor-%08u.bin", dir, sequence) >= (int)sizeof(path))
       return;
-   FILE *file = fopen(path, "ab");
+   FILE *file = fopen(path, "wb");
    if (file == NULL)
       return;
    const uint32_t header[] = { 0x48434d56, 1, kind, type, binding, element, bytes };
diff --git a/src/intel/vulkan/anv_shader.c b/src/intel/vulkan/anv_shader.c
index 281696f..f322fea 100644
--- a/src/intel/vulkan/anv_shader.c
+++ b/src/intel/vulkan/anv_shader.c
@@ -314,6 +314,19 @@ get_shader_trueos_helioc_bind_map_text(const struct anv_shader *shader)
    return stream_data;
 }
 
+static char *
+get_shader_trueos_helioc_devinfo_text(const struct anv_device *device)
+{
+   char *stream_data = NULL;
+   size_t stream_size = 0;
+   FILE *stream = open_memstream(&stream_data, &stream_size);
+   fprintf(stream, "TRUEOS_HELIOC_DEVINFO_V1 pci_device_id=0x%04x ver=%u verx10=%u\n",
+           device->physical->info.pci_device_id, device->physical->info.ver,
+           device->physical->info.verx10);
+   fclose(stream);
+   return stream_data;
+}
+
 static char *
 get_shader_bind_map_text(const struct anv_device *device,
                          const struct anv_shader *shader)
@@ -454,6 +467,17 @@ anv_shader_get_executable_internal_representations(
    }
    free(trueos_bind_map);
 
+   char *trueos_devinfo = get_shader_trueos_helioc_devinfo_text(device);
+   if (trueos_devinfo != NULL) {
+      vk_outarray_append_typed(VkPipelineExecutableInternalRepresentationKHR, &out, ir) {
+         VK_COPY_STR(ir->name, "TRUEOS HelioC devinfo");
+         VK_COPY_STR(ir->description, "Capture-only exact Mesa Intel device info");
+         if (!write_ir_text(ir, trueos_devinfo))
+            incomplete_text = true;
+      }
+   }
+   free(trueos_devinfo);
+
    vk_outarray_append_typed(VkPipelineExecutableInternalRepresentationKHR, &out, ir) {
       VK_COPY_STR(ir->name, "TRUEOS HelioC prog data");
       VK_COPY_STR(ir->description,
diff --git a/src/intel/vulkan/genX_cmd_buffer.c b/src/intel/vulkan/genX_cmd_buffer.c
index 99ee5ce..738d1d8 100644
--- a/src/intel/vulkan/genX_cmd_buffer.c
+++ b/src/intel/vulkan/genX_cmd_buffer.c
@@ -23,6 +23,7 @@
 
 #include <assert.h>
 #include <stdbool.h>
+#include <stdatomic.h>
 #include <stdio.h>
 
 #include "anv_private.h"
@@ -43,7 +44,8 @@
 #include "genX_cmd_draw_generated_flush.h"
 
 /* State bytes are emitted after descriptor lookup and BT address addition.
- * They remain non-relocatable until a higher layer proves ownership. */
+ * Per-record files avoid interleaved writes; the bytes remain non-relocatable. */
+static atomic_uint trueos_helioc_command_sequence = 0;
 static void
 anv_trueos_helioc_dump_cmd_state(uint32_t kind, mesa_shader_stage stage,
                                  const void *data, uint32_t bytes)
@@ -51,10 +53,11 @@ anv_trueos_helioc_dump_cmd_state(uint32_t kind, mesa_shader_stage stage,
    const char *dir = os_get_option("TRUEOS_HELIOC_ANV_DUMP_DIR");
    if (dir == NULL || data == NULL || bytes == 0)
       return;
+   const unsigned sequence = atomic_fetch_add(&trueos_helioc_command_sequence, 1);
    char path[4096];
-   if (snprintf(path, sizeof(path), "%s/helioc-anv-state.bin", dir) >= (int)sizeof(path))
+   if (snprintf(path, sizeof(path), "%s/helioc-anv-command-%08u.bin", dir, sequence) >= (int)sizeof(path))
       return;
-   FILE *file = fopen(path, "ab");
+   FILE *file = fopen(path, "wb");
    if (file == NULL)
       return;
    const uint32_t header[] = { 0x48434d56, 1, kind, stage, 0, 0, bytes };
@@ -2972,9 +2975,6 @@ genX(cmd_buffer_flush_descriptor_sets)(struct anv_cmd_buffer *cmd_buffer,
       }
    }
 
-   anv_trueos_helioc_dump_cmd_state(5, MESA_SHADER_NONE, cmd_buffer->batch.start,
-                                    cmd_buffer->batch.next - cmd_buffer->batch.start);
-
    return flushed;
 }
 
@@ -4108,6 +4108,15 @@ end_command_buffer(struct anv_cmd_buffer *cmd_buffer)
 
    anv_cmd_buffer_end_batch_buffer(cmd_buffer);
 
+   if (!list_is_singular(&cmd_buffer->batch_bos)) {
+      fprintf(stderr, "TRUEOS_HELIOC_CAPTURE: command trace incomplete: chained or multiple owned batch BOs\n");
+   } else {
+      const uint32_t bytes = (uint8_t *)cmd_buffer->batch.next -
+                             (uint8_t *)cmd_buffer->batch.start;
+      anv_trueos_helioc_dump_cmd_state(5, MESA_SHADER_NONE,
+                                       cmd_buffer->batch.start, bytes);
+   }
+
    return VK_SUCCESS;
 }
 
