# This file is intentionally not used by the server anymore.
# Keep it out of service startup; remoteAI now computes a deterministic bearer
# token from host identity internally.
# The file is preserved only for backward-compatibility notes.
