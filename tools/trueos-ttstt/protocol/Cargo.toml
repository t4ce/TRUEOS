[package]
name = "trueos-ttstt-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MIT"
description = "Versioned localhost wire protocol shared by trueos-ttstt and its clients"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
